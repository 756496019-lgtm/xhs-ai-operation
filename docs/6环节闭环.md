# 6 环节闭环图

> Mermaid 源文件，可直接粘进 GitHub README 或 PPT（用 mermaid 渲染插件）。

```mermaid
flowchart LR
    subgraph 输入层 ["素材输入层"]
        A1[多源爬虫<br/>14 个 scraper] 
        A2[对标账号监控<br/>Agent-Reach OpenCLI]
    end

    subgraph 选题层 ["选题层"]
        B1[规则热度评分<br/>0-100]
        B2[AI 精排 Top5<br/>qwen-turbo]
        B3[历史复盘反哺<br/>analyzer + qwen-max]
    end

    subgraph 生产层 ["生产层"]
        C1[01 素材采集 skill]
        C2[02 文案 skill]
        C3[03 配音 skill<br/>Edge TTS 9 音色]
        C4[04 剪辑 skill<br/>AI 帧匹配]
        C5[05 封面 skill]
        C6[06 图文笔记 skill]
    end

    subgraph 改写层 ["改写层"]
        D1[文字改写]
        D2[视频脚本]
        D3[fact_check 事实核查<br/>qwen-max]
    end

    subgraph 发布层 ["发布层"]
        E1[草稿箱]
        E2[定时发布]
        E3[多平台导出<br/>小红书/B站/公众号]
    end

    subgraph 复盘层 ["复盘层"]
        F1[创作中心 CSV<br/>手动导出]
        F2[SQLite 时间序列]
        F3[互动率 / 时段 / 标题模式分析]
        F4[周复盘卡片<br/>render_xhs_cards 双风格]
        F5[下期选题 prompt.md]
    end

    A1 --> B1 --> B2
    A2 --> B3
    F3 --> B3
    B2 --> C1 & C2
    B3 --> C2

    C1 --> C2 --> C3 --> C4
    C2 --> C5 & C6

    C2 --> D1
    C2 --> D2
    D1 --> D3
    D2 --> D3
    D3 --> E1

    C4 & C5 & C6 --> E1
    E1 --> E2 --> E3

    E3 --> F1
    F1 --> F2 --> F3 --> F4
    F3 --> F5
    F5 -.反哺.-> C2

    style F5 fill:#E8820C,color:#fff
    style D3 fill:#4ADE80,color:#000
    style B3 fill:#A78BFA,color:#fff
```

## 关键回路

1. **素材→选题→生产→发布**：常规线性链路，由 WorkBuddy 通过 6 个 skill 自动串起
2. **复盘反哺选题**：`analytics` 的 `next_week_prompt.md` 输出回到 `02-script-writer` 的输入，闭环成立
3. **改写前必经 fact_check**：避免编造数字（W17/W21 踩坑总结）

## 与赛题的对应

| 赛题环节 | 本图层级 |
|---|---|
| 素材库构建 | 输入层 |
| 选题生成 | 选题层 + 复盘层反哺 |
| 内容生产 | 生产层 |
| 内容改写 | 改写层 |
| 发布计划 | 发布层 |
| 数据回收分析 | 复盘层 |

## 颜色编码

- 橙色（`#E8820C`）：闭环回路的关键节点（选题反哺）
- 绿色（`#4ADE80`）：质量保障节点（事实核查）
- 紫色（`#A78BFA`）：AI 决策节点（含历史数据驱动）
